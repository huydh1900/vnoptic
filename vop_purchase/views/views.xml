<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="vop_purchase.list">
      <field name="name">vop_purchase list</field>
      <field name="model">vop_purchase.vop_purchase</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </list>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="vop_purchase.action_window">
      <field name="name">vop_purchase window</field>
      <field name="res_model">vop_purchase.vop_purchase</field>
      <field name="view_mode">list,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="vop_purchase.action_server">
      <field name="name">vop_purchase server</field>
      <field name="model_id" ref="model_vop_purchase_vop_purchase"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="vop_purchase" id="vop_purchase.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="vop_purchase.menu_1" parent="vop_purchase.menu_root"/>
    <menuitem name="Menu 2" id="vop_purchase.menu_2" parent="vop_purchase.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="vop_purchase.menu_1_list" parent="vop_purchase.menu_1"
              action="vop_purchase.action_window"/>
    <menuitem name="Server to list" id="vop_purchase" parent="vop_purchase.menu_2"
              action="vop_purchase.action_server"/>
-->
  </data>
</odoo>
