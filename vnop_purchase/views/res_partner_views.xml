<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_partner_tree_purchase_ref" model="ir.ui.view">
        <field name="name">res.partner.list.purchase.ref</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='complete_name']" position="before">
                <field name="ref" string="Mã NCC" optional="show"
                       column_invisible="context.get('res_partner_search_mode') != 'supplier'"/>
            </xpath>
        </field>
    </record>

</odoo>